[project]
name = "ecap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "docker>=7.2.0",
    "jupyter>=1.1.1",
    "pylance>=8.0.0",
    "ruff>=0.15.22",
]
